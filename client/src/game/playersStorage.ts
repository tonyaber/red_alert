import { IRegisteredPlayerInfo } from "../../../server/src/dto";

const players: IRegisteredPlayerInfo[] = []

const appState = {
    players: players,
    colors: ['#f00', '#ff0', '#00f', '#0f0', '#ffa500']
}
export default appState